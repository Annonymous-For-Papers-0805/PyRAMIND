# Pinned to the versions used when running the benchmarks reported in the paper.
# Python 3.10+ recommended.

numpy>=1.26,<3
requests>=2.32,<3
scikit-learn>=1.3,<2

# Paper figures (Mermaid/Plotly are used by chart scripts under paper/).
plotly>=5.18,<6
kaleido==0.2.1

# Naive RAG (BM25) baseline — paper Table 2 reference system.
rank-bm25>=0.2.2,<1

# OPTIONAL — paper-default embedding backend (BAAI/bge-large-en-v1.5).
# Install only if you set `embedding_backend="bge"`; the Azure backend is
# the cost-friendly drop-in alternative and does not require this dep.
# sentence-transformers>=2.7,<5
